name: Catalog Compute

on:
  schedule:
    - cron: '47 */6 * * *'
  workflow_run:
    workflows: ["Scheduled Discovery"]
    types: [completed]
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/compute.yml'
      - 'compute.py'
      - 'coverage_metrics.py'
      - 'merge_compute.py'
      - 'requirements-compute.txt'
      - 'src/geoip_shadow.py'
      - 'src/pre_admission.py'
      - 'src/security.py'
      - 'tests/test_geoip_shadow.py'
      - 'tests/test_pre_admission.py'
      - 'tests/test_compute_merge.py'
      - 'tests/test_coverage_telemetry.py'
      - 'tests/test_country_ranking_retrieval_source.py'
      - 'tests/test_security.py'

concurrency:
  group: catalog-main-write
  cancel-in-progress: false

permissions:
  contents: write
  actions: read

jobs:
  prepare-geoip-shadow:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.event_name == 'push' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 4
    outputs:
      available: ${{ steps.prepare.outputs.available }}
    steps:
      - name: Download and verify server-country MMDB
        id: prepare
        shell: bash
        run: |
          set -u
          mkdir -p geoip-shadow
          DATA_URL='https://github.com/sapics/ip-location-db/releases/download/latest/server-country.mmdb'
          SUM_URL='https://github.com/sapics/ip-location-db/releases/download/checksum/server-country.mmdb.sha256'
          available=false
          if curl --fail --location --silent --show-error --retry 3 --retry-all-errors \
              --connect-timeout 15 --max-time 90 --max-filesize 20000000 \
              --proto '=https' --tlsv1.2 "$DATA_URL" -o geoip-shadow/server-country.mmdb \
            && curl --fail --location --silent --show-error --retry 3 --retry-all-errors \
              --connect-timeout 15 --max-time 30 --max-filesize 4096 \
              --proto '=https' --tlsv1.2 "$SUM_URL" -o geoip-shadow/server-country.mmdb.sha256; then
            expected="$(awk 'NF {print tolower($1); exit}' geoip-shadow/server-country.mmdb.sha256)"
            actual="$(sha256sum geoip-shadow/server-country.mmdb | awk '{print tolower($1)}')"
            bytes="$(stat -c '%s' geoip-shadow/server-country.mmdb)"
            if [[ "$expected" =~ ^[0-9a-f]{64}$ ]] \
              && [[ "$actual" == "$expected" ]] \
              && (( bytes >= 1000000 && bytes <= 20000000 )); then
              printf 'sha256:%s\n' "$actual" > geoip-shadow/release.txt
              available=true
            fi
          fi
          if [[ "$available" != true ]]; then
            rm -f geoip-shadow/server-country.mmdb geoip-shadow/server-country.mmdb.sha256 geoip-shadow/release.txt
            echo 'Shadow GeoIP unavailable; legacy compute will continue unchanged.'
          fi
          echo "available=$available" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@v4
        if: steps.prepare.outputs.available == 'true'
        with:
          name: geoip-shadow
          path: |
            geoip-shadow/server-country.mmdb
            geoip-shadow/release.txt
          retention-days: 1
          if-no-files-found: error

  shard:
    needs: prepare-geoip-shadow
    if: always() && (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.event_name == 'push' || github.event.workflow_run.conclusion == 'success')
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1, 2, 3, 4, 5, 6, 7]
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install local MMDB reader
        if: needs.prepare-geoip-shadow.outputs.available == 'true'
        run: python -m pip install --disable-pip-version-check -r requirements-compute.txt
      - uses: actions/download-artifact@v4
        if: needs.prepare-geoip-shadow.outputs.available == 'true'
        with:
          name: geoip-shadow
          path: geoip-shadow
      - name: Unit tests
        run: python -m unittest discover -s tests -v
      - name: Passive pre-admission shard
        shell: bash
        run: |
          set -euo pipefail
          shadow_args=()
          if [[ -s geoip-shadow/server-country.mmdb && -s geoip-shadow/release.txt ]]; then
            shadow_args=(
              --geo-shadow-mmdb geoip-shadow/server-country.mmdb
              --geo-shadow-provider sapics-server-country
              --geo-shadow-release "$(cat geoip-shadow/release.txt)"
            )
          fi
          python compute.py \
            --shard "${{ matrix.shard }}" \
            --shards 8 \
            --max-sources 40 \
            --workers 2 \
            --geo-max-new 25 \
            "${shadow_args[@]}" \
            --output "compute-artifacts/shard-${{ matrix.shard }}.json"
      - uses: actions/upload-artifact@v4
        with:
          name: compute-shard-${{ matrix.shard }}
          path: compute-artifacts/shard-${{ matrix.shard }}.json
          retention-days: 1
          if-no-files-found: error

  merge:
    needs: shard
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - uses: actions/download-artifact@v4
        with:
          pattern: compute-shard-*
          path: compute-results
          merge-multiple: false
      - name: Snapshot previous global node index
        run: |
          if [ -f exports/nodes_deduplicated.json ]; then
            cp exports/nodes_deduplicated.json "$RUNNER_TEMP/nodes-before.json"
          else
            printf '{"nodes":[]}\n' > "$RUNNER_TEMP/nodes-before.json"
          fi
      - name: Merge, globally deduplicate, and rank safe compute outputs
        run: >-
          python merge_compute.py
          --artifacts compute-results
          --top-per-country 30
          --country-export-limit 500
          --max-per-source 5
      - name: Build aggregate coverage telemetry
        run: >-
          python coverage_metrics.py
          --artifacts compute-results
          --previous-nodes "$RUNNER_TEMP/nodes-before.json"
          --expected-shards 8
      - name: Snapshot generated data hashes
        run: |
          {
            sha256sum \
              data/sources.json \
              data/node_index.json \
              data/geo_cache.json \
              exports/prechecked_sources.json \
              exports/nodes_deduplicated.json \
              exports/country_handoff.json \
              exports/country_handoff_v4.json \
              exports/coverage_metrics.json
            find exports/countries -maxdepth 1 -type f -name '*.json' -print0 \
              | sort -z \
              | xargs -0 sha256sum
          } > "$RUNNER_TEMP/catalog-generated.sha256"
      - name: Validate generated data
        run: python -m unittest discover -s tests -v
      - name: Verify tests did not mutate generated data
        run: sha256sum -c "$RUNNER_TEMP/catalog-generated.sha256"
      - name: Commit meaningful compute changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/sources.json data/node_index.json data/geo_cache.json \
            exports/prechecked_sources.json exports/nodes_deduplicated.json \
            exports/country_handoff.json exports/country_handoff_v4.json \
            exports/coverage_metrics.json
          git add -A exports/countries
          if git diff --cached --quiet; then
            echo "No compute changes."
          else
            git commit -m "chore(catalog): compute update [skip ci]"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
